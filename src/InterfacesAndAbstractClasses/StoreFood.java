package InterfacesAndAbstractClasses;

public interface StoreFood {
  //interface is default public
  //interfaces are all public and their all abstract cause you can't class the interface directly?
  //By default it's abstract?
//  void keepFresh(){
//
//  }
  //This is saying this is the behavior of what goes into this interface is going to be defined, so all of them storefood, but they will do them differently?
  void keepFresh();
}
